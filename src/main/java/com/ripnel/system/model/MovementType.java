package com.ripnel.system.model;

public enum MovementType {
    IN,        // entrada al stock
    OUT,       // salida del stock
    TRANSFER   // transferencia entre ubicaciones (lo podemos dejar para después)
}
